---

language: c
sudo: false 
script: dpkg-buildpackage
sudo: required
dist: trusty
before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test
  - sudo apt-get -qq update
  - sudo apt-get install debhelper fakeroot gcc
